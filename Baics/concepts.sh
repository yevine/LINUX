Echo is a Unix/Linux command tool used for displaying lines of text or string which are passed as arguments on the command line.
 way to comment in Bash
 using : ‘ operator.

: '
Code or Comment here
 '
  using : <<
 <<comment
 "Code" or "Comments"
comment

if [condition];then
  cmds
else
 cmds
fi

if [condition];then
  cmds
elif
 cmds
fi
### os name cmd, UNAME for--- linux & mac  VER --Windows

if [condition];then
  cmds
elif
 cmds
fi
